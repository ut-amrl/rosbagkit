[project]
name = "GQ-devkit"
version = "0.1.0"
description = "devkit for GQ dataset"
readme = "README.md"

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
